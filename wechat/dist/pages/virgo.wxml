
  <view>
    <view class="box">
      <image class="border" src="{{killer}}"/>
    </view>
    <view>
      <audio class="width" poster="{{shangwenjie}}" name="{{name}}" author="{{author}}" src="{{music}}" controls loop/>
    </view> 
  </view>